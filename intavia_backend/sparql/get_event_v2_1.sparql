PREFIX bds:  <http://www.bigdata.com/rdf/search#>
PREFIX crm: <http://www.cidoc-crm.org/cidoc-crm/>
PREFIX bioc: <http://ldf.fi/schema/bioc/>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX idm: <https://www.intavia.eu/idm/>
PREFIX idmcore: <http://www.intavia.eu/idm-core/>
PREFIX owl: <http://www.w3.org/2002/07/owl#>

SELECT ?event ?role ?entity ?event_label ?role_label ?begin ?end ?time_span_label ?role_type

{% for dataset in datasets %}
FROM <{{dataset.value}}>
{% endfor %}

WHERE {
BIND(<{{event_id}}> as ?event)
{% include 'retrieve_events_v2_1.sparql' %}
}